module.exports = {
  // Use 2 spaces for indentation
  tabWidth: 2,
  useTabs: false,

  // Use single quotes for strings
  singleQuote: true,

  // Add trailing commas where valid in ES5 (objects, arrays, etc.)
  trailingComma: 'es5',

  // Print semicolons at the ends of statements
  semi: true,

  // Line length that the printer will wrap on
  printWidth: 80,

  // Format quoted code embedded in markdown
  embeddedLanguageFormatting: 'auto',

  // Put > of a multi-line JSX element at the end of the last line
  bracketSameLine: false,

  // Use single quotes instead of double quotes in JSX
  jsxSingleQuote: true,

  // Control how Prettier formats quoted code embedded in markdown
  proseWrap: 'preserve',

  // End of line character
  endOfLine: 'lf',
};
